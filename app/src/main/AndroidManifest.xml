<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectfinal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerViewActivity"></activity>
        <activity android:name=".UserSettingActivity" />
        <activity android:name=".LoginPage.OnLogAct" />
        <activity android:name=".LoginPage.OpLogAct" />
        <activity android:name=".LoginPage.ABnLogAct" />
        <activity android:name=".LoginPage.ABpLogAct" />
        <activity android:name=".LoginPage.BnLogAct" />
        <activity android:name=".LoginPage.BpLogAct" />
        <activity android:name=".LoginPage.AnLogAct" />
        <activity android:name=".LoginPage.ApLogAct" />
        <activity android:name=".OnegActivity" />
        <activity android:name=".OposActivity" />
        <activity android:name=".ABnegActivity" />
        <activity android:name=".ABposActivity" />
        <activity android:name=".BnegActivity" />
        <activity android:name=".BposActivity" />
        <activity android:name=".AnegActivity" />
        <activity android:name=".AposActivity" />
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".UserMapActivity"
            android:label="@string/title_activity_user_map" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>